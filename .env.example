# Public site URL used for canonicals, sitemap, robots, OpenGraph, and structured data.
# Set this in Vercel → Project → Settings → Environment Variables (Production + Preview).
NEXT_PUBLIC_SITE_URL=https://talhazain.com
